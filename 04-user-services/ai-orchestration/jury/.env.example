# The Jury — environment. Copy to `.env` (git-ignored) and fill in.
# Never commit the real key. See repo CLAUDE.md "No secrets in git".

FIREWORKS_API_KEY=CHANGE_ME

# Optional: override the defaults baked into jury.py's flags.
# JURY_MODEL=accounts/fireworks/models/kimi-k3
# JURY_BASE_URL=https://api.fireworks.ai/inference/v1
