# provenance: A · handoff brief supplied by the founder (Gemini-drafted), 2026-08-08 · 2026-08-08 · verify: fill from the employer's Azure portal; real values live in .env (git-ignored), never here
#
# Copy to .env (git-ignored) and fill in. Never commit the real values.

# Azure ML Configuration
AZURE_SUBSCRIPTION_ID="your_subscription_id"
AZURE_RESOURCE_GROUP="your_resource_group"
AZURE_WORKSPACE_NAME="your_workspace_name"

# Azure OpenAI Configuration (if using LLMs)
AZURE_OPENAI_API_KEY="CHANGE_ME"
AZURE_OPENAI_ENDPOINT="https://your-resource.openai.azure.com/"
