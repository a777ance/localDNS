{
  "_comment": "Open-PR snapshot for tools/hlidskjalf.py. O-tier: captured via the GitHub API by a session that can reach it (the agent proxy blocks most REST paths, so the seat is blind to PRs without this file). The ref list ages while you read it; so does this — the board prints its age and raises a decision when it goes stale. Refresh with: search_pull_requests `is:open user:a777ance`, then rewrite this file.",
  "captured": "2026-08-11T14:40:00Z",
  "via": "GitHub MCP search_pull_requests `is:open user:a777ance` (org-wide, single query)",
  "repos": {
    "localDNS": {
      "open_prs": [
        {"number": 31, "head": "promote/promotion-lock", "title": "Promote the top-gate lock into main (cherry-pick, lock-only)"},
        {"number": 30, "head": "claude/loki-norn-promotion-e9e8vf", "title": "Codex IX: the Loki Norn — site the promotion instead of accepting it"},
        {"number": 11, "head": "cloudflare/workers-autoconfig", "title": "Add Cloudflare Workers configuration"}
      ]
    }
  }
}
