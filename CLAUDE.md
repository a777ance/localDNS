# CLAUDE.md

Briefing for Claude Code. Everything is in README.md — setup, rationale, deploy paths, topology, known issues, and verification. Read that first.

**The live t630 is the source of truth.** When in doubt, SSH to `192.168.1.118`.

Every commit to `main` must leave README.md able to reproduce a working system on clean Ubuntu 24.04. Use feature branches for half-finished work.
