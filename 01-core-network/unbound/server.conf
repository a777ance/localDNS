# Deploys to: /etc/unbound/unbound.conf.d/server.conf
# Reload: sudo systemctl restart unbound

server:
    verbosity: 1
    interface: 127.0.0.1 #bind Unbound to loopback only — Pi-hole (network_mode: host) reaches it at 127.0.0.1#5335; nothing on the LAN queries :5335 directly, so do not expose it to all interfaces
#    interface: 0.0.0.0 #all interfaces — only needed if Unbound moves into a Docker bridge container (then Pi-hole must reach it across the bridge)
#    interface: 172.17.0.1 #docker bridge loopback
    port: 5335
    do-ip4: yes
    do-ip6: no
    do-udp: yes
    do-tcp: yes

    root-hints: "/var/lib/unbound/root.hints"

    access-control: 127.0.0.0/8 allow
    access-control: 192.168.0.0/16 allow
    access-control: 10.0.0.0/8 allow
    access-control: 172.16.0.0/12 allow

    hide-identity: yes
    hide-version: yes
    harden-glue: yes
    harden-dnssec-stripped: yes
    use-caps-for-id: yes
